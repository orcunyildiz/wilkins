# C++ task libraries for flow-control examples (stateful and stateless).
# Orchestrated by the Python 'wilkins-master' entry point.
add_subdirectory            (stateful)
add_subdirectory            (stateless)
